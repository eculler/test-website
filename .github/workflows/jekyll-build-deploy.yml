name: Jekyll site CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build Jekyll for GitHub Pages
      uses: actions/jekyll-build-pages@v1.0.12
            
